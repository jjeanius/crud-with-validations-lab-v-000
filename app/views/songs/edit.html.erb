<%= form_tag "/song" do %>
  Title: <%= text_field_tag @song.title %><br>
  Artist Name: <%= text_field_tag @song.artist_name %>
  Release Year: <%= text_field_tag @song.release_year %>
  Released: <%= text_field_tag @song.released %>
  Genre: <%= text_field_tag @song.genre %>

  <%= button_to "Create Person" %>

<% end %>
